//XXX not in use 

// "use server";

// import { signIn  } from "@/auth";
// // import { Button } from "@/components/ui/button";

// export async function actionSignIn( provider?: string | undefined ) {
// // export async function actionSignIn({ provider, ...props }: { provider?: string } & React.ComponentPropsWithRef<typeof Button>) {
//     console.log("SighIn: provider =>", provider );
//     await signIn(provider);
// }